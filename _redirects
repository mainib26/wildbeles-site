# www redirect is handled at DNS level via CNAME

# Hamus business claim landing: every /hamus/business/<id> serves the page,
# which reads the id client-side and fetches the business.
/hamus/business/*  /hamus/business/index.html  200
